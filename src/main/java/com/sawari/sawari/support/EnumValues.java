package com.sawari.sawari.support;

public class EnumValues {
    public enum TripStatusEnum {
        Requested,
        Accepted,
        Cancelled,
        Active,
        Completed,
    }
    public enum PaymentStatusEnum {
        Pending,
        Succeeded,
        Failed
    }
}
